BIND_ADDRESS=0.0.0.0:3000

# Postgres configuration (optional; falls back to in-memory store when unset)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/alloy
DATABASE_MAX_CONNECTIONS=10
DATABASE_MIN_CONNECTIONS=1

# JWT signing secret for tickets (use a strong, random 32+ byte string in production)
ALLOY_TICKET_SECRET=replace-with-strong-secret
